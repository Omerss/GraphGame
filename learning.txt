qLearning

action_space = 1 of 4 options\
    s_t= a_t-1


Q(a_t-1, a_t) = Q(a_t-1, a_t) + alpha[reword_t + gamma*max(Q)]

reword_t = number of new item added to system after the action a_t



epsiode:
for t=1: 6
select a_t
calc reword_t
update Q

a_0 is one option that we always choose
Parameters:
